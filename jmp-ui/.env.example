# Базовый URL инстанса Jitsi Web (используется на HomePage в «Создать встречу» / «Подключиться»)
VITE_JITSI_URL=http://localhost:8000

# URL API бэкенда (по умолчанию /api/v1 через прокси Vite)
# VITE_API_URL=/api/v1
